appenders:
  stdout:
    encoder:
      pattern: "{d(%H:%M:%S)} | {({l}):5.5} | {f}:{L} | {m}{n}"
    kind: console

  # Appender for the step strategy
  file:
    kind: file
    path: /home/eli/Github/plugins/toolbox.log
    encoder:
      pattern: "{d(%H:%M:%S)} | {({l}):5.5} | {f}:{L} | {m}{n}"

root:
  level: trace
  appenders:
    - stdout
    - file
