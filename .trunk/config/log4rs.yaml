appenders:
  stdout:
    encoder:
      pattern: "{d(%H:%M:%S)} | {({l}):5.5} | {f}:{L} | {m}{n}"
    kind: console

  file:
    kind: file
    path: { REPLACE_THIS_WITH_PATH_WHERE_YOU_WANT_LOGFILE }
    encoder:
      pattern: "{d(%H:%M:%S)} | {({l}):5.5} | {f}:{L} | {m}{n}"

root:
  level: trace
  appenders:
    - stdout
    - file
