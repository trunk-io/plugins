version: 0.1
actions:
  definitions:
    - id: npm-check
      display_name: NPM Check
      runtime: node
      packages_file: package.json
      triggers:
        - schedule: 24h
        - files: ["package.json", "package-lock.json"]
      run: pwd
      run_from: ${linter}
