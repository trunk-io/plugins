version: 0.1
actions:
  definitions:
    - id: hello-world
      display_name: Hello World
      description: Say hello to the world pre-commit using python
      runtime: python
      run: ${cwd}/hello
      triggers:
        - git_hooks: [pre-commit]
