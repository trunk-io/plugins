version: 0.1
# IfChange
required_trunk_version: ">=1.21.1-beta.2"
# ThenChange tests/repo_tests/config_check.test.ts

environments:
  - name: SYSTEM
    environment:
      - name: PATH
        list: ["${env.PATH:-}"]
