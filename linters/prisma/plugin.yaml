version: 0.1
lint:
  definitions:
    - name: prisma
      tools: [prisma]
      files: [prisma]
      known_good_version: 4.16.1
      commands:
        - name: prisma format
          output: rewrite
          success_codes: [0]
          in_place: true
          batch: true
          formatter: true
          run: prisma format --schema=${target}
